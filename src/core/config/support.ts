/**
 * WirePusher device identifiers are intentionally client-visible in the
 * temporary client-side delivery adapter. They can trigger notification spam,
 * but never grant access to report details or application data.
 */
export const PLATFORM_SUPPORT_ADMIN_EMAIL = 'm.osmandonmezyurek@gmail.com';
export const WIREPUSHER_ADMIN_DEVICE_ID = 'q2P4mpzk3';
export const WIREPUSHER_NOTIFICATION_TYPE = 'dijitalstok';
